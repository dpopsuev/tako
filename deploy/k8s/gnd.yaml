apiVersion: apps/v1
kind: Deployment
metadata:
  name: gnd
  labels:
    app: origami-gnd
spec:
  replicas: 1
  selector:
    matchLabels:
      app: origami-gnd
  template:
    metadata:
      labels:
        app: origami-gnd
    spec:
      containers:
        - name: gnd
          image: origami-gnd:latest
          args: ["--port", "9100", "--drivers", "git,docs"]
          ports:
            - containerPort: 9100
          livenessProbe:
            httpGet:
              path: /healthz
              port: 9100
            initialDelaySeconds: 5
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /readyz
              port: 9100
            initialDelaySeconds: 3
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: gnd
spec:
  selector:
    app: origami-gnd
  ports:
    - port: 9100
      targetPort: 9100
