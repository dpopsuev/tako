kind: Schematic
# Child circuit: used as a sub-circuit by subcircuit.yaml
# Tests: standalone circuit that can be delegated to via instrument: circuit.
circuit: child
start: process
done: child-done

nodes:
  - name: process
    instrument: transformer
    action: passthrough
    description: "Sub-circuit processing step"

edges:
  - id: process-done
    from: process
    to: child-done
