kind: Schematic
circuit: ref-hexagonal
start: ingest
done: done
nodes:
  - name: ingest
    instrument: transformer
    action: echo
    description: "Receives input, passes through"
  - name: process
    instrument: transformer
    action: echo
    description: "Processes data, passes through"
  - name: output
    instrument: transformer
    action: echo
    description: "Produces final output"

edges:
  - id: e1
    from: ingest
    to: process
  - id: e2
    from: process
    to: output
  - id: e3
    from: output
    to: done
