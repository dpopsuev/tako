circuit: memory-test
description: "Simple 2-node circuit for verifying cross-walk MemoryStore persistence"
handler_type: transformer

nodes:
  - name: process
    approach: rapid
    handler: echo
  - name: finish
    approach: analytical
    handler: echo

edges:
  - id: E1
    name: proceed
    from: process
    to: finish
    when: "true"
  - id: E2
    name: done
    from: finish
    to: _done
    when: "true"

start: process
done: _done
