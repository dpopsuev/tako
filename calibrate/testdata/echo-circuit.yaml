circuit: echo-test
description: Minimal circuit for testing CLI dispatch calibration
start: prompt
done: done
handler_type: transformer

calibration:
  outputs:
    - field: "prompt.answer"
      scorer_name: "answer"

nodes:
  - name: prompt
    handler: core.llm
    prompt: echo-prompt.txt

edges:
  - id: prompt-done
    from: prompt
    to: done
