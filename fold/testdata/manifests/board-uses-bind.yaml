apiVersion: tako/v1
kind: Board
metadata:
  name: consumer
spec:
  uses:
    alpha:
      kind: Schematic
      module: github.com/example/schematic-a
    datasource:
      kind: Component
      module: github.com/dpopsuev/tako-components/rp
    mcp:
      kind: Component
      module: github.com/dpopsuev/tako/instruments/mcp
  bind:
    alpha:
      data: datasource
      transport: mcp
  domains: [ocp/ptp]
  domain_serve:
    port: 9300
    assets:
      circuits:
        alpha: circuits/alpha.yaml
