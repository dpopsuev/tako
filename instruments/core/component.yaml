kind: Component
component: tako-core
namespace: core
version: "1.0.0"
description: Built-in transformers for the Origami circuit framework
provides:
  transformers: [llm, http, jq, file]
requires:
  tako: ">=1.0.0"
