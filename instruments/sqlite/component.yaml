kind: component
component: origami-sqlite
namespace: sqlite
version: "1.0.0"
description: SQLite DSL component — YAML-defined schema, migrations, parameterized CRUD
provides:
  transformers: []
  hooks: []
requires:
  origami: ">=0.3.0"
satisfies: []